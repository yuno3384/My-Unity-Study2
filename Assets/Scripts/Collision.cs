using UnityEngine;
// 충돌체에 부딪혀서 소멸 , 땅에 충돌하여 멈추는 것> 서 있는 것
// 예) 포탈 : 영역안에 들어가면 이동하는
public class Collision : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
